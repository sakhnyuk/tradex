export { default } from './Chart';

// import { connect } from 'react-redux';
// import FourChart from './Chart';

// import { setInterval } from '../../store/modules/tradingCore/reducer';
// import { selectLayout, selectLayout, selectInterval, selectLayoutIntervals } from '../../store/chart/selectors';

// export default connect(
//   state => ({
//     layout: selectLayout(state),
//     layoutIntervals: selectLayoutIntervals(state),
//     interval: selectInterval(state),
//   }),
//   { setLayoutInterval, setInterval },
// )(FourChart);
